services:
  chart-ocr:
    build: .
    restart: always
    ports:
      - "5050:5050"
    volumes:
      - ./uploads:/app/uploads
      - ./extracted_output:/app/extracted_output
    networks:
      - web

networks:
  web:
    external: true
